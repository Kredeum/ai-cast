{
  "name": "@ai-cast/foundry",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf node_modules out broadcast cache .turbo ",
    "chain": "./anvil.sh",
    "format": "forge fmt",
    "check": "solhint src/**/*.sol",
    "build": "forge compile",
    "test": "forge test",
    "abis": "ts-node mergeAbis.ts",
    "func": "forge script script/send/AiCastRequest.s.sol --fork-url sepolia --sender $TESTNET_SENDER --account $TESTNET_ACCOUNT --broadcast",
    "func2": "forge script script/send/SendRequest.s.sol --fork-url sepolia --sender $TESTNET_SENDER --account $TESTNET_ACCOUNT --broadcast",
    "deploy:all": "forge script script/deploy/DeployAll.s.sol",
    "deploy:deploy": "pnpm deploy:all --fork-url $CHAIN --account $ACCOUNT --broadcast",
    "deploy:validate": "pnpm deploy:all --fork-url $CHAIN && pnpm run abis",
    "deploy:anvil": "export CHAIN=anvil SENDER=$ANVIL_SENDER ACCOUNT=$ANVIL_ACCOUNT && pnpm run chain && pnpm run deploy:deploy --password '' && pnpm run deploy:validate",
    "deploy:testnet": "SENDER=$TESTNET_SENDER ACCOUNT=$TESTNET_ACCOUNT pnpm run deploy:deploy --verify && pnpm run deploy:validate",
    "deploy:mainnet": "SENDER=$MAINNET_SENDER ACCOUNT=$MAINNET_ACCOUNT pnpm run deploy:deploy --verify && pnpm run deploy:validate",
    "deploy:sepolia": "CHAIN=sepolia pnpm run deploy:testnet",
    "deploy:optimism": "CHAIN=optimism pnpm run deploy:mainnet"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@chainlink/env-enc": "^1.0.5",
    "@chainlink/functions-toolkit": "^0.3.1",
    "@types/fs-extra": "^11.0.4",
    "@types/node": "^22.4.1",
    "eslint-plugin-json": "^4.0.1",
    "ethers": "^5.7.2",
    "fs-extra": "^11.2.0",
    "prettier": "^3.3.3",
    "solhint": "^5.0.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.4"
  }
}